{
  "name": "samarakoon-agro-pos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:api": "cd apps/api && php artisan serve --host=127.0.0.1 --port=8000",
    "dev:desktop": "cd apps/desktop && npm start",
    "dev": "concurrently -k -n API,DESKTOP \"npm run dev:api\" \"npm run dev:desktop\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
